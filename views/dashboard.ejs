<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <%- include('partials/header') %>
  <% if (typeof successMessage !== 'undefined') { %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= successMessage %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% } %>
  
  <% if (typeof errorMessage !== 'undefined') { %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
      <%= errorMessage %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% } %>
  
    <h1 class="mb-4">👋 Willkommen im Dashboard, <%= email %>!</h1>
    <div class="mt-4">
      <h2>E-Mails abrufen</h2>
      <form action="/fetch-emails" method="POST" class="row g-3">
        <div class="col-auto">
          <input type="date" name="sinceDate" class="form-control" required>
        </div>
        <div class="col-auto">
          <button type="submit" class="btn btn-primary mb-3">E-Mails abrufen</button>
        </div>
      </form>
    </div>
    
    <h2 class="mt-4">Status deiner Abfrage</h2>
<p><%= imapStatus %></p>

<h2 class="mt-4">Gefundene Leads</h2>

<% if (leads.length > 0) { %>
  <% leads.forEach(function(lead) { %>
    <div class="card my-3 p-3">
      <h5><%= lead.subject %> (von <%= lead.from %>)</h5>
      <ul class="list-group mt-2">
        <% for (const [key, value] of Object.entries(lead.data)) { %>
          <li class="list-group-item">
            <strong><%= key %>:</strong> <%= value %>
          </li>
        <% } %>
        <a href="/import-customer/<%= lead.data.leadId %>" class="btn btn-success btn-sm">
          Kunde importieren
        </a>
        <a href="/import-lead/<%= lead.data.leadId %>" class="btn btn-primary btn-sm">
          Lead importieren
        </a>



        


      </ul>
    </div>
  <% }); %>
<% } else { %>
  <p>Keine Leads gefunden.</p>
<% } %>




  <div class="card p-4">
    <h2>Dein Bereich</h2>
    <p>Hier kannst du später deine IMAP-Daten, fincrm-API-Token und persönliche Einstellungen verwalten.</p>

    <div class="mt-4">
        <a href="/settings" class="btn btn-primary">Einstellungen bearbeiten</a>
        <a href="/logout" class="btn btn-danger ms-2">Logout</a>
      </div>
  </div>
  <%- include('partials/footer') %>

</body>
</html>
