<!--vies/sendEmail.ejs-->

<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>E-Mail an <%= leadEmail %> senden</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <%- include('partials/header') %>
  <h1 class="mb-4">E-Mail an <%= leadEmail %> senden</h1>

  <% if (successMessage) { %>
    <div class="alert alert-success"><%= successMessage %></div>
  <% } %>
  <% if (errorMessage) { %>
    <div class="alert alert-danger"><%= errorMessage %></div>
  <% } %>

  <form action="/email/send" method="POST">
    <input type="hidden" name="to"       value="<%= leadEmail %>">
    <div class="mb-3">
      <label class="form-label">Betreff</label>
      <input type="text" name="subject" class="form-control" required>
    </div>
    <div class="mb-3">
      <label class="form-label">Nachricht</label>
      <textarea name="message" class="form-control" rows="6" required>
Hallo [Name],

ich hoffe, es geht Ihnen gut. Hier eine ausführliche Nachricht…
      </textarea>
    </div>
    <input type="hidden" name="customerName" value="<%= leadName %>">
    <button type="submit" class="btn btn-primary">Senden</button>
    <a href="/dashboard" class="btn btn-secondary ms-2">Abbrechen</a>
  </form>

  <%- include('partials/footer') %>
</body>
</html>

  <!--vies/sendEmail.ejs Ende-->

